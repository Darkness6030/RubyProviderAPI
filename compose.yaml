services:
  generator:
    build: .
    volumes:
      - .:/workspace
    working_dir: /workspace
    command:
      - --spec
      - examples/novapay/openapi.yaml
      - --provider
      - novapay
      - --overrides
      - examples/novapay/overrides.yaml
      - --lang
      - ruby
      - --output
      - output
      - --docs-output
      - output
      - --verify
